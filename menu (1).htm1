<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Меню Европейской Кухни</title>
    <style>
        body {
            font-family: 'Segoe UI', sans-serif;
            background-color: #fefefe;
            margin: 0;
            padding: 0;
        }

        header {
            background-color: #2e2e2e;
            color: white;
            padding: 20px 0;
            text-align: center;
        }

        .language-switcher {
            text-align: right;
            padding: 10px 20px;
        }

        .language-switcher button {
            margin-left: 10px;
            padding: 5px 10px;
            cursor: pointer;
        }

        .menu {
            max-width: 1000px;
            margin: 20px auto;
            padding: 20px;
        }

        .menu-section {
            margin-bottom: 40px;
        }

        .menu-section h2 {
            border-bottom: 2px solid #ccc;
            padding-bottom: 10px;
        }

        .menu-item 
            display: flex;
            align-items:
